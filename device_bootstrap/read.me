1. Install system dependencies first

   sudo apt update && sudo apt --yes --force-yes install dnsmasq hostapd python3-dev python3-pip

2. 

3. Run docker

   docker run -it --privileged \
      -v /garage_settings:/garage_settings \
      bootstrap_server


